{
  "name": "kph-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "type-check": "turbo type-check"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "optionalDependencies": {
    "lightningcss-linux-x64-gnu": "1.32.0"
  }
}
